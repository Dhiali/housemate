# Environment variables for Housemate backend
# Copy to .env and adjust as needed

PORT=8080

# MySQL config for Google Cloud SQL
DB_HOST=your_cloud_sql_ip_address
DB_USER=your_db_username
DB_PASSWORD=your_strong_database_password
DB_NAME=housemate_db
DB_PORT=3306

# JWT Secret - Use minimum 32 characters
JWT_SECRET=your_super_strong_jwt_secret_minimum_32_characters

# Frontend URL for CORS
FRONTEND_URL=https://your-frontend-domain.com

# Production Security Notes:
# - Use Google Secret Manager for production credentials
# - Never commit actual credentials to version control
# - Rotate passwords regularly
# - Use strong, unique passwords for each environment
